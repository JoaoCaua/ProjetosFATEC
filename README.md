# Projetos
git clone https://github.com/JoaoCaua/ProjetosFATEC.git

git config --global user.name "JoaoCaua"

git config --global user.email "joao.laureano@fatec.sp.gov.br"

mvn spring-boot:run

http://localhost:8080/swagger-ui/index.html#/

http://localhost:8080/h2

git init

git remote add origin https://github.com/JoaoCaua/ProjetosFATEC.git

git add .

git commit -m "primeiro commit"

git push -u origin master
